from tqdm import tqdm
import math

from lib import *

def get_possible_a(x):


def brute_force(n):

	for x in tqdm(range(1, n)):

		a_i = []

def get_nested(x, y, z):
	return math.sqrt(x + math.sqrt(y) + math.sqrt(z))


def g(x, y, z):
	return 0

def f(n):
	pass

if __name__ == "__main__":
	
	test_f(f)
	

